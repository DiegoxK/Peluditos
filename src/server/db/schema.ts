import { ObjectId } from "mongodb";
import { z } from "zod";

// export const PostSchema = z.object({
//   _id: z.instanceof(ObjectId).optional(),
//   name: z.string(),
//   createdAt: z.date(),
//   userId: z.string(),
// });
